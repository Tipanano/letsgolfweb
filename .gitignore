# Node modules
node_modules/

# Server folder (separate git repo)
golf-game-server/

# OS files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/

# Environment files
.env
.env.local

# Logs
*.log
npm-debug.log*

# Build output
dist/
build/
# Browser-suite screenshots: write-only diagnostics, regenerated on every run.
# Nothing reads them back — every one is a bare page.screenshot() call — so
# they only ever produced a binary diff after each run, and the seven that
# were tracked drifted a whole HUD redesign out of date before anyone noticed.
# They are still written locally for eyeballing after a run.
tests/*.png
